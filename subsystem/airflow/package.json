{
  "name": "@subsystem/airflow",
  "version": "0.1.0",
  "description": "Simple4Decision's Airflow",
  "scripts": {
    "airflow-init": "mkdir -p ./dags ./logs ./plugins ./config && echo \"AIRFLOW_UID=$(id -u)\" >> .env && sudo docker compose up postgres-client && sudo docker compose up airflow-init -d",
    "airflow-install": "sudo docker compose up -d",
    "docker-logs": "sudo docker compose logs"
  },
  "devDependencies": {
    "@ttoss/config": "^1.32.10",
    "@types/node": "20.16.0",
    "@types/validator": "^13.11.9",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
