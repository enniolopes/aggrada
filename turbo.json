{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "SSH_HOST",
    "SSH_PORT",
    "SSH_USERNAME",
    "SSH_PASSWORD",
    "DATABASE_HOST",
    "DATABASE_HOST_READ_ONLY",
    "DATABASE_NAME",
    "DATABASE_PASSWORD",
    "DATABASE_PORT",
    "DATABASE_USERNAME"
  ],
  "pipeline": {
    "//#lint": {
      "outputs": []
    },
    "env": {
      "outputs": [".env"]
    }
  }
}
