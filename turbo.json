{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "SSH_HOST",
    "SSH_PORT",
    "SSH_USERNAME",
    "SSH_PASSWORD",
    "DATABASE_HOST",
    "DATABASE_HOST_READ_ONLY",
    "DATABASE_NAME",
    "DATABASE_PASSWORD",
    "DATABASE_PORT",
    "DATABASE_USERNAME",
    "DATABASE_URL",
    "REMOTE_HOST",
    "REMOTE_IP",
    "REMOTE_PORT",
    "REMOTE_USER",
    "REMOTE_PASSWORD",
    "REMOTE_DIR",
    "REMOTE_PORT_SERVER",
    "REMOTE_USER_SERVER",
    "REMOTE_PASSWORD_SERVER",
    "JWT_SECRET",
    "PORT",
    "GITLAB_REPO_URL",
    "GITLAB_ACCESS_TOKEN",
    "REGISTRY_USERNAME",
    "REGISTRY_PASSWORD",
    "HOSTNAME",
    "AUTH_TOKEN"
  ],
  "tasks": {
    "lint": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "build": {
      "outputs": ["build/**"]
    }
  }
}
